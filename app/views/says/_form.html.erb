<%= form_for(@say) do |f| %>
  <% if @say.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@say.errors.count, "error") %> prohibited this say from being saved:</h2>

      <ul>
      <% @say.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :body %><br>
    <%= f.text_field :body %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
